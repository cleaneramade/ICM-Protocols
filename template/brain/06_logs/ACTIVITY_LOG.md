# Activity Log

A git-style, append-only record of meaningful file and rule changes, so there
is always a clear before/after history of what the brain did. Newest entries
at the bottom. Secrets (passwords, tokens, keys) must be stripped before
logging.
